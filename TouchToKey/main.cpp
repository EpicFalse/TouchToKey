#include <Windows.h>

#include "MainTouchWindow.h"

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow)
{
	CMainTouchWindow* touchWindow = CMainTouchWindow::GetInstance();
}